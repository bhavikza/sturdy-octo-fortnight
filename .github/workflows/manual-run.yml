name: Ntfy Triggered Workflow

on:
  repository_dispatch:
    types: [ntfy_trigger]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run Hello World script
      run: python hello-world.py
