name: Hello World on NTFY Trigger

on:
  repository_dispatch:
    types: [ntfy_trigger]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Hello World script
        run: echo "Hello, World! Triggered by NTFY event."
